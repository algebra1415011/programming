

			8
			c++;
			cout<<"YES"<<endl;
			exit(0);
		if(s[i]!=s[len])
		{
		{
		}
		}
	cin.tie(0),cerr.tie(0),ios_base::sync_with_stdio(0);
	cin>>s;
	for(ll i=0;i<s.length()/2;i++)
	if(s.length()==1)
	ll len=s.length()-1;	
	return 0;
	string s;
	{
	}			
#define ll long long int
#include <bits/stdc++.h>
int main()
using namespace std;
{
}